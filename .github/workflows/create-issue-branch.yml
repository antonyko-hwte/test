name: Create Issue Branch and PR

on:
  issues:
    types: [opened, assigned, labeled]
  issue_comment:
    types: [created]
  pull_request:
    types: [closed]

# 권한 설정
permissions:
  contents: write
  pull-requests: write
  issues: write
  checks: write

jobs:
  create_issue_branch_job:
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'issues' && (
        github.event.action == 'opened' ||
        github.event.action == 'assigned' ||
        github.event.action == 'labeled'
      ) &&
      !contains(github.event.issue.labels.*.name, 'docs') &&
      !contains(github.event.issue.labels.*.name, 'release') &&
      !contains(github.event.issue.labels.*.name, 'sub_issue') &&
      !contains(github.event.issue.labels.*.name, 'duplicate') &&
      !contains(github.event.issue.labels.*.name, 'invalid') &&
      !contains(github.event.issue.labels.*.name, 'question') &&
      !contains(github.event.issue.labels.*.name, 'wontfix') &&
      !contains(github.event.issue.labels.*.name, 'env')

    steps:
      - name: Create prefixed branch
        id: create_branch
        uses: robvanderleek/create-issue-branch@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # branches 매개변수로 레이블-prefix 매핑
          branches: |
            [
              {"label": "bug", "prefix": "bugfix/"},
              {"label": "feature", "prefix": "feature/"},
              {"label": "hotfix", "prefix": "hotfix/"},
              {"label": "refactor", "prefix": "refactor/"},
              {"label": "*", "prefix": ""}
            ]
          
          # 브랜치 이름 템플릿
          branchName: "${branch.prefix}chistock-#${{ github.event.issue.number }}"
          
          openDraftPR: true
          copyIssueLabelsToPR: true
          copyIssueAssigneeToPR: true
          draftPRTitle: "[${branch.prefix.replace('/', '')}] chistock-#${{ github.event.issue.number }}: ${{ github.event.issue.title }}
      
      - name: Create and push branch
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # 브랜치 생성 및 푸시
          git checkout -b "${{ steps.branch.outputs.name }}"
          git push -u origin "${{ steps.branch.outputs.name }}"
      
      - name: Create Pull Request
        id: create_pr
        uses: actions/github-script@v6
        with:
          script: |
            const issue = context.payload.issue;
            const branchName = '${{ steps.branch.outputs.name }}';
            
            // PR 생성
            const pr = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `chistock-#${issue.number}: ${issue.title}`,
              head: branchName,
              base: 'main',
              body: `## 📌 관련 이슈\nResolves #${issue.number}\n\n## 📝 설명\n${issue.body || '이슈 해결을 위한 변경사항입니다.'}\n\n---\n*자동 생성된 PR입니다.*`,
              draft: true
            });
            
            // 라벨 복사
            if (issue.labels && issue.labels.length > 0) {
              const labels = issue.labels.map(label => label.name);
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: pr.data.number,
                labels: labels
              });
            }
            
            // Assignee 복사
            if (issue.assignees && issue.assignees.length > 0) {
              const assignees = issue.assignees.map(assignee => assignee.login);
              await github.rest.issues.addAssignees({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: pr.data.number,
                assignees: assignees
              });
            }
            
            return pr.data.number;
      
      - name: Update issue with branch info
        uses: actions/github-script@v6
        with:
          script: |
            const issue = context.payload.issue;
            const branchName = '${{ steps.branch.outputs.name }}';
            const prNumber = '${{ steps.create_pr.outputs.result }}';
            
            // 이슈에 코멘트 추가
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              body: `🎉 **브랜치 및 PR이 생성되었습니다!**\n\n` +
                    `**브랜치**: \`${branchName}\`\n` +
                    `**PR**: #${prNumber}\n\n` +
                    `해당 브랜치에서 작업을 시작해주세요.`
            });
